# Lua Craft · Proof approval A/B

Research prototype for UEWD.X490 Session 2. Two approaches to the proof-approval screen:
`/a` side-by-side, `/b` guided one-at-a-time, `/` links both and resets state.

Built from `BUILD_PLAN.md` — read that first. `npm install && npm run dev`.
Screens for reference in `docs/screens/`. Drop a real proof at `public/proof.png` to replace the placeholder.
